' Define loft solid
With Loft
    .Reset
    .Name "{{name}}"
    .Component "{{component}}"
    .Material "{{material}}"
    .Tangency "{{tangency}}"
    .Minimizetwist "{{minimizetwist}}"
    .CreateNew
End With
